# FastAPI Auth Service

A modern authentication microservice built with FastAPI, SQLAlchemy and PostgreSQL. Provides JWT-based authentication, user management, and secure password change functionality.

## Features

- JWT authentication (access/refresh tokens)
- User registration and login
- Protected endpoints (e.g., `/api/v1/users/me`)
- Change password with validation
- Secure password hashing
